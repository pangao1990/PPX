# 变更记录

## 6.0.0

PPX V6 是全新项目结构，不兼容 V5，也不提供 V5 原地迁移。

### 架构

- 重新规划为 `ppx-py` 与 `ppx-js` 两个包：Python 运行、CLI 和三端打包归入前者，浏览器 RPC 归入后者。
- 新增 `ppx.toml` 与 `ppx.lock`，集中管理应用信息、框架版本和兼容契约。
- 用 `Application` 组合替代 V5 的 API 多继承和全局静态 `Config`。
- Python 只暴露 `call(method, params)`；业务方法通过 `@api_method` 显式注册。
- 框架存储迁移到应用数据目录，并使用临时文件和原子替换写入。
- 开发者项目只暴露 `api/`、`gui/`、`ppx/assets/`、配置与锁文件；不再提供或要求修改框架入口、spec、iss、DMG/deb 脚本。
- 开源仓库顶层归并为 `api/`、`gui/`、`ppx/`，两个包与维护工具都收进 `ppx/`。

### 修复

- 应用更新支持 GitHub 资产标签中的 CPU 架构，保留既有文件名时也能拒绝错误架构的安装包。

- 修复 Windows 上构建、初始化和更新流程无法直接定位 `pnpm.cmd` 的问题，使用解析后的可执行路径。

- 升级 Pillow 至 12.3.0，修复图像处理依赖中的已知安全漏洞；最低 Python 版本提高到 3.10。

- 修复英文 Windows 环境中中文 CLI 日志写入管道时触发的编码异常；构建脚本默认启用 UTF-8，并增加重定向输出回归测试。

### 品牌与在线打包

- 恢复原版多色 LOGO，客户端与介绍站使用一致的蓝、绿、金黄配色。
- 移除品牌、页脚、入门模板和普通说明中的多余版本标记，保留兼容性与真实版本信息。
- 保留 GitHub Actions 自动和手动三端打包，补齐 Linux GTK/PyGObject 及 Windows 安装器准备。
- 候选安装包按系统和架构上传，保留 14 天，并附带 SHA-256；没有文件时任务失败。
- V5 源码入口改为已公开的 V5.3.4 标签，避免尚未推送的维护分支产生 404。

### 示例工作台与可靠性修复

- 重做响应式工作台，增加 Python 通信、文件与目录选择、可持久化便签、最近调用和浏览器预览状态。
- 应用更新支持明确的检查、下载、后台、取消和完成状态；校验后由用户选择打开安装包。
- 客户端入门入口改为内置离线文档，避免未部署的在线文档路径返回 404；在线 V5 内容明确区分。
- 页面切换时回到顶部并移动键盘焦点；窄窗口保留文档和 GitHub 入口，错误提示自动滚入可见区域。
- 侧栏支持滚动，修复低高度窗口中文档和 GitHub 按钮落在视区外且无法点击的问题。
- 下载失败或取消后可直接重试，关闭更新弹窗后仍可重新打开已下载的安装包。
- 外链打开失败时显示可复制的网址，空路径调用返回参数错误，避免意外打开工作目录。
- 修复 `timeoutMs: 0` 立即超时、就绪事件监听泄漏，以及某个订阅者异常阻断其他订阅者的问题。
- 防止并发下载竞争同一临时文件；取消状态贯穿检查和重试，文件关闭后再清理，避免 Windows 删除失败。
- 拒绝明确不匹配 CPU 架构的安装包，避免单候选错误兜底。
- 前端仅暴露 `call` 入口；Python 注册和事件方法保留在内部。
- 开发服务固定监听回环地址和配置端口，端口冲突立即报错，退出时回收启动的进程树。
- 修复窗口显示前存储目录尚未创建导致的首次保存失败。
- 修正示例 Prettier 配置与 ESM 不兼容的问题。
- V6 文档补充工作台逐步教程，V5 正文和旧 URL 保留，额外归档原首页并增加完整侧栏入口。

### 开发者体验

- 新增 `ppx new`、`ppx init`、`ppx icon`、`ppx doctor`、`ppx update`、`ppx dev`、`ppx build`。
- `ppx new --frontend` 可生成 Vanilla、Vue 或 React 项目，均不包含框架源码；`ppx dev` 自动协调 Vite 与 pywebview。
- `ppx icon` 从一张方形主图生成 macOS、Windows、Linux 图标；`ppx doctor --json` 提供 CI/IDE 可读诊断结果。
- `ppx update` 支持 `--check`、`--dry-run` 和 `--to`。
- 兼容更新只处理框架包和锁文件；不兼容更新在修改前停止。
- 应用入口、PyInstaller spec 和三端安装器元数据全部在 `build/cache/` 动态生成。
- `ppx build` 在当前平台生成并验证正式安装包；`--console` 仅生成调试应用。
- 打包时不再因图标工具间接收集完整 Pillow；业务代码主动使用 Pillow 时仍会照常收集。
- 新增 `pnpm run check`，一次执行环境诊断、全量测试和发布源码一致性检查。
- VS Code 默认隐藏依赖、虚拟环境、构建产物和缓存，并配置 Python 框架源码分析路径。

### 更新与安全

- 应用版本改用语义化比较，修复 `5.10.0` 与 `5.9.9` 一类字符串比较错误。
- 安装包按平台和 CPU 架构选择。
- 支持 GitHub Release SHA-256 校验、`.part` 下载和原子替换。
- 安装包和框架更新清单改为强制校验 SHA-256，并拒绝不安全的资源文件名。
- 修正下载进度、失败清理和取消下载行为。
- 桥接错误增加稳定错误码和请求 ID，避免直接暴露 Python 异常。
- 调用前先校验业务 API 参数，业务代码内部的 `TypeError` 不再被误报或泄露；JavaScript 端统一包装底层桥接异常。
- `ppx-js` 增加 TypeScript 声明、SSR 安全导入和默认 RPC 超时。
- Python RPC 支持直接注册 `async def`，并在跨线程事件循环场景中安全取得结果。
- JavaScript RPC 使用单一总超时预算，并在发起调用前生成 requestId，超时和传输错误也可追踪。
- 新增业务资源目录、用户数据目录辅助函数，以及保存文件对话框和窗口状态/控制 RPC。
- 修复 Linux 空 `XDG_DATA_HOME` 与 Windows 空 `APPDATA` 的目录回退，避免把数据写进当前工作目录。
- 内置存储严格要求字符串键和标准 JSON 值，拒绝 `NaN`、集合等跨语言不兼容数据。
- Python 返回不可 JSON 序列化的数据时返回稳定的 `INVALID_RESULT`，不再退化为模糊的桥接错误。
- 窗口增加可调整大小、全屏、置顶、关闭确认和背景色配置，并严格验证布尔值与颜色。
- `ppx update` 可修复同版本包漂移，并明确拒绝依赖降级和错误更新通道。
- `ppx update` 的事务回滚支持自定义前端目录；应用名、反向域名标识和安装器元数据增加跨平台安全校验。

### 依赖

- Vue 3.5.42、Vite 8.2.2、Element Plus 2.14.5。
- pywebview 6.2.1、PyInstaller 6.22.2。
- packaging 26.3、Pillow 11.3.0、setuptools 82.0.1、nodemon 3.1.14。
- Node.js 最低版本提高到 22.13.0；pnpm 固定为 11.25.0，npm-run-all2 升级到 9.0.3，Sass 升级到 1.104.0。
- Ubuntu 22.04 继续使用 PyGObject 3.48.2；新版本需要 GLib 2.80，不满足该 LTS 的系统基线。
- macOS + Python 3.9 固定 PyObjC 11.1，避免解析到已不兼容该解释器的 PyObjC 12 源码构建。
- Element Plus 组件和图标改为按需导入，减少生产包体积。
- 移除 V6 不再使用的 TinyDB 和 cryptography 默认依赖；内置键值存储改为原子 JSON。
- 统一仓库和两个待发布包的许可证元数据为 AGPL-3.0-only，并在发行包中携带许可证全文。

### CI 与质量

- 跟踪根目录和 GUI 的 pnpm lockfile。
- 更新 GitHub Actions 主版本。
- 增加桥接、配置、存储、框架更新兼容性、业务目录保护、安装包校验和版本一致性测试。
- 增加发布产物一致性检查脚本和 PyPI/npm 分阶段发布说明。

## 5.3.4

V5 的完整历史和源码请查看 `v5` 分支。
